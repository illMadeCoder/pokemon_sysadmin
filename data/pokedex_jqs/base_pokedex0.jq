{
    id:.[0].id,
    name:.[0].name,
    types:.[0].types,
    // sprite:[0].sprites.front_default,
    // height:[0].height,
    // weight:[0].weight,        
    // flavor_text:.[1].flavor_text_entries[0].flavor_text,
    // stats:[0].stats,
}
